# provider-alerts
This repository gives you the tools to import data from raw csv files, normalize it and transform it into a final CSV file that'll feed an alert-generator to inform discrepancies from different provider's performances
